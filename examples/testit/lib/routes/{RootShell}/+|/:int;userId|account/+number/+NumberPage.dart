import 'package:testit/file_router.dart';

class NumberPage extends StatelessPage<NumberPageRoute> {
  const NumberPage(super.route, {super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
